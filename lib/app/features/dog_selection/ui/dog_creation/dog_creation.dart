import "package:flutter/material.dart";

class DogCreation extends StatelessWidget {
  const DogCreation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
